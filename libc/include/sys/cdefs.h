#ifndef _SYS_CDEFS_H
#define _SYS_CDEFS_H 1

#define __simpleos_libc 1
/* Further macros could be put here, but we do not use this yet.
 * This exists in case we need to create our own libc extension. */
#endif
